package com.skyflux.kiln.user.config;

import org.springframework.context.annotation.Configuration;

/** User module configuration placeholder. Bean wiring that isn't component-scanned lives here. */
@Configuration
class UserModuleConfig {
}
