spring:
  application:
    name: kiln-user-test
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

logging:
  level:
    org.flywaydb: INFO
    com.skyflux.kiln: DEBUG
