# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
*.iws
.vscode/
# IntelliJ output dirs — anchored per-module, NOT bare `out/`
# (bare pattern accidentally matched `adapter/out/` Hexagonal sub-package).
app/out/
auth/out/
common/out/
infra/out/
user/out/

# jOOQ generated
infra/build/generated-src/jooq/

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db

# Local config
application-local.yml
application-local.properties

# Docker
/data/
.env

# Claude Code skills — unpacked skill content is reproducible from
# skills-lock.json via `/reload-plugins`. Commit the lock file, ignore
# the unpacked directories (analogous to package-lock.json vs node_modules/).
.agents/
.claude/skills/
/skills/
