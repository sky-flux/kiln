pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

rootProject.name = 'kiln'

include 'app'
include 'common'
include 'infra'
include 'user'
include 'auth'
